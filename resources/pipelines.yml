resources:
  pipelines:
    cmeg_dlt_pipeline:
      name: cmeg_dlt_pipeline
      catalog: ${var.catalog_name}
      target: ${var.schema_name}
      photon: true
      serverless: true
      channel: PREVIEW
      development: true
      continuous: false
      libraries:
        - notebook:
            path: ../_resources/_dlt_pipeline.py
      configuration:
        cmeg.catalog: ${var.catalog_name}
        cmeg.schema: ${var.schema_name}
